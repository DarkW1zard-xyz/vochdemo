{
  "name": "vochdemo-workspace",
  "private": true,
  "scripts": {
    "dev": "npm run dev:setup && npm --prefix sql run dev",
    "dev:setup": "npm --prefix sql install && npm --prefix sql run prisma:generate",
    "build": "cd sql && npm run build",
    "search:evaluate": "cd sql && npm run search:evaluate",
    "benchmark:throughput": "cd sql && npm run benchmark:throughput",
    "benchmark:resources": "cd sql && npm run benchmark:resources",
    "presentation:generate": "node scripts/generate-presentation.mjs"
  },
  "devDependencies": {
    "pptxgenjs": "^4.0.1"
  }
}
